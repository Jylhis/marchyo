import QtQuick

// Filled in by the emoji/clipboard task. The function stubs keep the
// LauncherWindow key routing lint-clean in the meantime.
Item {
    property string query: ""
    implicitHeight: 0

    function move(delta) {
    }

    function activate() {
    }
}
