---
title: AI
description: The bring-your-own-key AI desktop wired to OpenRouter.
---

TODO — the AI layer for users. Cover:

- what BYOK means (you supply an OpenRouter key; nothing is sent without it)
- the clients installed (pi, claude-code) and how task→model routing works
- setting the key safely (sops-nix secret, never in the store)
- what's optional (context layer, MCP tools, agent skills)

Adapt from `site/src/content/docs/docs/configuration/ai.mdx` — keep the secret
handling prominent. This is a marchyo extra with no omarchy equivalent.
