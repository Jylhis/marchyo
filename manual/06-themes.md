---
title: Themes
description: The Jylhis dark/light theme, switching variants, and font scaling.
---

TODO — theming from the user's side. Cover:

- the single Jylhis design system: Field (dark) and Sheet (light)
- switching variant at runtime with `marchyo theme` (no rebuild) vs setting it
  declaratively (`marchyo.theme.variant`)
- font scaling (`marchyo.theme.fontScale`)
- what's themed consistently across apps, terminal, and boot splash

Adapt from `site/src/content/docs/docs/configuration/theming.mdx` (keep it
user-facing — no `tokens.json` internals).
