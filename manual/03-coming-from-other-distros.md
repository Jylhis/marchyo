---
title: Coming from Other Distros
description: Orientation for people arriving from Arch, omarchy, macOS, or Windows.
---

TODO — the "translate what you already know" chapter. Cover:

- **From omarchy / Arch:** what's the same (Hyprland, the aesthetic), what's
  different (declarative config, no `pacman`/AUR, the app-launch bind namespace).
- **From macOS / Windows:** the mental shift to a tiling compositor, keyboard-first
  navigation, where "settings" live.
- the golden rule: never install things imperatively — change the config instead.
