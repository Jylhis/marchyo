---
title: Hardware Authentication
description: Fingerprint and FIDO2/U2F unlock on supported hardware.
---

TODO — passwordless-ish auth. Cover:

- fingerprint unlock (`marchyo.security.fingerprint.enable`) and how hyprlock
  follows it automatically
- FIDO2 / U2F security keys (`marchyo.security.fido2.enable`), enrolling with
  `pamu2fcfg`
- what needs the right hardware and what's off by default (and why)
