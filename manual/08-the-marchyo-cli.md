---
title: The Marchyo CLI
description: The `marchyo` command — menus, toggles, capture, theme, and more.
---

TODO — the CLI tour for users. Cover:

- what `marchyo` is (the single command behind the binds, menus, and waybar actions)
- the useful everyday subcommands (theme, capture, menus, toggles, reminders, info)
- that the command/flag/exit-code contract is frozen until 2.0 (safe to script)

Adapt from `site/src/content/docs/docs/usage/cli.mdx` (the full reference); keep this
chapter to the commands a person types day-to-day.
