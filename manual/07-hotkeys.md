---
title: Hotkeys
description: The complete keyboard map — apps, windows, capture, menus, and toggles.
---

TODO — the reference chapter (largest). Cover the full bind list, grouped:

- app launches (`Super+<letter>`) and web apps (`Super+Shift+<letter>`)
- window & workspace management
- capture: screenshot, screen record, color picker, OCR
- menus & power (`Super+Escape`, `Super+Alt+Space`)
- clipboard (`Super+C/V/X`), emoji, launcher
- system toggles (nightlight, DND, idle-lock, dictation, cursor zoom)

Adapt directly from `site/src/content/docs/docs/usage/hotkeys.mdx` — keep it in
sync. Remember marchyo uses `Super+<letter>` for app launches (omarchy uses
`Super+Shift+<letter>`).
